<!DOCTYPE html>

<html>
  <head>
    <title><%= content_for(:title) || "Party" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <meta name="apple-mobile-web-app-capable" content="yes">

    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%# = tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">

    <link rel="icon" href="/icon.svg" type="image/svg+xml">

    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module", nonce: true %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body data-controller="connection-monitor">
    <div class="container"><%= yield %></div>

    <dialog
      data-connection-monitor-target="dialog"
      class="border-0 p-4 rounded-3 shadow-lg"
      style="max-width: 500px;"
    >
      <div class="mb-3">
        <h1 class="fs-5 mb-3">
          <i class="bi bi-wifi-off me-2"></i>
          Connection Lost
        </h1>
        <p class="mb-0">
          You've lost connection to the server. The page will automatically refresh when the connection is restored.
        </p>
      </div>
    </dialog>
  </body>

  <div class="container">
    <footer
      class="
        d-flex flex-wrap justify-content-between align-items-center py-3
        my-4 border-top
      "
    >
      <div class="col-md-4">
        <span class="mb-3 mb-md-0 text-muted">© 2025 Jeremy Nicklas</span>
      </div>
      <ul class="nav col-md-4 justify-content-end list-unstyled d-flex">
        <li class="ms-3">
          <a class="text-muted" href="https://github.com/nickjer/party">
            <i class="bi bi-github" style="font-size: 1.5rem;"></i>
          </a>
        </li>
      </ul>
    </footer>
  </div>
</html>
