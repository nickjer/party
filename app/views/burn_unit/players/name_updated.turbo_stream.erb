<%# locals: (players:, current_player:, player:) %>
<%= turbo_stream.update "players" do %>
  <%= render(partial: "burn_unit/players/player",
    collection: players,
    as: :player,
    locals: { current_player: }) %>
<% end %>
<%= turbo_stream.update_all "[data-player-name-id='#{player.id}']", player.name %>
