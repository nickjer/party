<%# locals: (question:) %>
<div class="card shadow-sm border-primary mb-3">
  <div class="card-header bg-primary text-white">
    <h5 class="mb-0"><i class="bi bi-question-circle me-2"></i>Question</h5>
  </div>
  <div class="card-body">
    <p class="card-text fs-5 mb-0"><%= question %></p>
  </div>
</div>
